services:
  landing-page:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gaussora-landing
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
      # Add other environment variables here if needed
      # - NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id
